<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VoiceRecorder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:statusBarColor">@color/statusColor</item>

        <item name="android:textColor">@color/colorText</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="rippleColor">@color/greyDarkDisable</item>
    </style>

    <style name="Theme.VoiceRecorder" parent="Base.Theme.VoiceRecorder" />
</resources>